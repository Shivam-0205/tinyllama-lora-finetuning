datasets:
  - roneneldan/TinyStories
  - wikitext/wikitext-2-raw-v1
  - ag_news
  - daily_dialog
  - squad

max_samples_per_dataset: 1000

tokenization:
  max_length: 128
  truncation: true
  padding: max_length
